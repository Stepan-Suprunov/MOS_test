export interface IHeadCell {
    id: number,
    key: string,
    label: string
};

export interface IDataCell {
    id: number,
    name: string,
    productCode: string,
    status: string,
    description: string
};